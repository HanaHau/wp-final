# 寵物記帳 APP - 專案文件

## 📋 專案概述

這是一個結合「記帳」與「寵物陪伴」的 Web 應用程式。使用者透過記帳與寵物互動，存錢會轉換為虛擬點數，可用於寵物消費。

## 🏗️ 技術架構

### 前端
- **框架**: Next.js 14 (App Router)
- **語言**: TypeScript
- **樣式**: Tailwind CSS
- **狀態管理**: Zustand
- **圖表**: Recharts
- **認證**: NextAuth.js (簡化 email 登入，開發模式)

### 後端
- **API**: Next.js API Routes
- **ORM**: Prisma
- **資料庫**: SQLite (本地開發)

### 部署建議（未來）
- **前端/後端**: Vercel
- **資料庫**: Supabase / Neon / Railway（生產環境建議使用 PostgreSQL）

## 📁 專案結構

```
wp-final/
├── app/                    # Next.js App Router
│   ├── api/               # API 路由
│   ├── (auth)/            # 認證相關頁面
│   ├── dashboard/         # 主畫面
│   ├── statistics/        # 統計頁面
│   ├── profile/           # 個人資料
│   └── pet/               # 寵物設定
├── components/            # React 元件
│   ├── ui/               # 基礎 UI 元件
│   ├── pet/              # 寵物相關元件
│   └── transaction/      # 記帳相關元件
├── lib/                  # 工具函數
│   ├── prisma.ts         # Prisma 客戶端
│   └── utils.ts          # 通用工具
├── prisma/               # Prisma schema
├── public/               # 靜態資源
└── types/                # TypeScript 類型定義
```

## 🗄️ 資料庫 Schema

### 核心資料表

1. **User** - 使用者
   - id, email, name, image, createdAt

2. **Pet** - 寵物
   - id, userId, name, imageUrl, points, fullness, mood, health, createdAt

3. **Transaction** - 記帳記錄
   - id, userId, amount, category, type (expense/income/deposit), date, note, createdAt

4. **Category** - 類別（預設 + 自訂）
   - id, userId, name, icon, isDefault, createdAt

5. **PetPurchase** - 寵物購買記錄
   - id, petId, itemName, cost, purchasedAt

## 🚀 開發步驟

### Phase 1: MVP 基礎功能
1. ✅ 專案初始化與配置
2. ⏳ 資料庫 Schema 設定
3. ⏳ 認證系統（Google OAuth）
4. ⏳ 記帳 CRUD API
5. ⏳ 主畫面與記帳功能
6. ⏳ 寵物基礎顯示

### Phase 2: 寵物互動系統
7. ⏳ 寵物狀態計算邏輯
8. ⏳ 寵物回應系統
9. ⏳ 點數購買功能

### Phase 3: 統計與進階功能
10. ⏳ 統計頁面（月曆、圓餅圖）
11. ⏳ 個人資料頁面
12. ⏳ 寵物設定頁面

## 📝 環境變數設定

建立 `.env.local` 檔案：

```env
# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"
```

**注意**: 目前使用 SQLite 資料庫（無需設定 DATABASE_URL），認證使用簡化的 email 登入（開發模式）。

## 🛠️ 安裝與執行

```bash
# 安裝依賴
npm install

# 初始化資料庫（SQLite）
npx prisma generate
npx prisma migrate dev

# 開發模式
npm run dev
```

**登入方式**: 在登入頁面輸入任意 email 即可登入（開發模式，會自動建立使用者）

## 📚 API 路由設計

### 記帳相關
- `GET /api/transactions` - 取得記帳列表
- `POST /api/transactions` - 新增記帳
- `PUT /api/transactions/[id]` - 更新記帳
- `DELETE /api/transactions/[id]` - 刪除記帳

### 寵物相關
- `GET /api/pet` - 取得寵物資訊
- `PUT /api/pet` - 更新寵物資訊
- `POST /api/pet/purchase` - 寵物購買物品

### 統計相關
- `GET /api/statistics/monthly` - 取得月統計
- `GET /api/statistics/categories` - 取得類別統計

## 🎯 MVP 優先順序

1. **認證系統** - 使用者登入
2. **記帳功能** - 基本 CRUD
3. **寵物顯示** - 基礎寵物資訊展示
4. **主畫面** - 整合記帳與寵物
5. **統計頁面** - 基本統計展示

