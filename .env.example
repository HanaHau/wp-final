# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Google OAuth (Optional - for Google Sign In)
# If you don't set these, the app will still work with email login
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Database (SQLite - no configuration needed for local development)
# DATABASE_URL is automatically handled by Prisma for SQLite

# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 3. Replace "your-secret-key-here" with the generated secret
# 4. (Optional) Set up Google OAuth credentials (see GOOGLE_AUTH_SETUP.md)
# 5. Replace Google OAuth placeholders with your actual credentials
